# Plain-text user store for the auth integration tests.
# Consumed by <user-properties ... plain-text="true"/> in infinispan-auth.xml,
# so SCRAM-SHA-1/256/512 (which need the cleartext password server-side) work.
# Format: <username>=<password>
testuser=testpassword
